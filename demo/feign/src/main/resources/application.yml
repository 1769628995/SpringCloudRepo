#启动端口
server:
  port: 8765
# 应用服务名
spring:
  application:
    name: service-feign
#开启feign
feign:
  hystrix:
    enabled: true
eureka:
  client:
    serviceUrl:
# 注册中心地址
      defaultZone: http://localhost:8761/eureka/


